#include "main.h"

/**
 * print_last_digit - Writes last digit of a number
 * @int: Input character
 * Return: value of last digit
 */

int print_last_digit(int)
{
	if (int lDigit % 10)
		return (lDigit);
}
