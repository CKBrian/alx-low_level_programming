#include "main.h"

/**
 * _abs - Writes absolute value of an integer
 * @int: Integer used to test for absolute value
 * Return: 0 always
 */

int _abs(int)
{
	if (int a > 0)
	{
		return (a);
	}
	else if (int a < 0)
	{
		return (-a);
	}
}
