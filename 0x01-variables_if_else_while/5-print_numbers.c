#include <stdio.h>

/**
 * main - Prints "digits 1 to 10 and a new line"
 *
 * Return: 0
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");

	return (0);
}
