#include "main.h"
/**
 * _strcmp - Compares two strings
 * @s1: First string
 * @s2: Second string
 * Return: int
 */
int _strcmp(char *s1, char *s2)
{
	int i, k;

	for (i = 0; s1[i] != '\0'; i++)
	{}
	for (k = 0; s2[k] != '\0'; k++)
	{}

}
