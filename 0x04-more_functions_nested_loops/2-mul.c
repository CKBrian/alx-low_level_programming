#include "main.h"

/**
 * mul - multiplies two intergers and returns result
 * @a: First integer to be multiplied
 * @b: Second value to be multiplied
 * Return: Product q
 */
int mul(int a, int b)
{
	int q;

	q = a * b;
	return (q);
}
